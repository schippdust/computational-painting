## Agent Generators

Agent generators create collections of agents in a systematic way. They initialize and draw the collections, then when their routines are complete they will destroy the agents / collections. These classes are generic, then are extended in the rendering components for specific functionality. They could also be implemented fully within the context of the sketch, but this is less desirable.
